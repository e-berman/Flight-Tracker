{
    "version": 2,
    "builds": [
        {
            "src": "./flight_tracker_controller.mjs",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "flight_tracker_controller.mjs",
        }
    ]
}
